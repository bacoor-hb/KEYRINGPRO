import { StyleSheet } from 'react-native'

const createStyles = () => {
  return StyleSheet.create({
    containerBox: {
      position: 'relative',
      overflow: 'hidden'
    }

  })
}

export default createStyles
